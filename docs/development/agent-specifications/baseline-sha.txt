Baseline SHA: 4def7fb3ab14724bb9cdd921c2c981f31ec8ae19
Branch: microcanonical-refactor

Legacy backends policy: exact fixed-(E,T) rejection/DP code lives in
menobis-test-oracles/src/legacy_fixed_et/ (reference oracle, compiled
and tested on every CI run).  No archive branch; the archive
branch/tag exact-fixed-total-v1 was deleted.

Current status after Phase 5 (fixed-(E,T) Gibbs migration):

Tests:
  cargo test -p menobis-core         = 280 passed (fast unit tests)
  cargo test -p menobis-test-oracles = 25 passed (enumeration + stationarity + legacy comparison)
  cargo clippy --workspace -D warnings = clean
  cargo fmt --all -- --check = clean

Microcanonical modules:

  fixed_et/         — fixed-(E,T) thin orchestrator calling Gibbs chain (no legacy DP)
  fixed_kt/         — fixed-degree support MCMC + Gibbs occupation allocation
  fixed_total/      — NEW (Phase 4): shared pair-Gibbs fixed-total chain
  fixed_strength/   — fixed-strength MCMC (ME direct + 4-cycle)
  mcmc/             — shared config/counters/outcome types
  support/          — shared uniform-edge sampling
  model/            — shared primitives (family, problem, sampling_plan)
  route/            — NEW: unified router (sample_microcanonical dispatch)

Note: complement mode in fixed_kt/sampler.rs is output-bound, not a
scalability bug: it triggers only for dense graphs (E > N²/2), so the
inversion is Θ(E) = Θ(N²) which is inherent to the dense output.

Reference oracles (menobis-test-oracles):
  enumeration.rs        — exact state enumeration + normalization
  legacy_fixed_et/      — legacy rejection/DP backends (scientific reference)
  tests/fixed_total_stationarity.rs   — Gibbs vs enumeration (TV < 0.03)
  tests/fixed_total_legacy_comparison.rs — Gibbs vs legacy DP (E=10)

Legacy code removed from production:
  - crates/menobis-core/src/.../fixed_et/{me,b,w,support}.rs
  - FixedETOccupancy trait and all impls
  - sample_positive_occupations, scaled_rejection_fallback, DP constants
  - FixedETError::{TableTooLarge, NoBackendAvailable}

Known limitations:
  - No SamplingPlan routing wired into Python entry points (Rust router exists)
  - Fixed (E,s) not implemented
  - Partial per-constraint mini-framework structure remains (fixed_strength/)
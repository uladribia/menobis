Baseline SHA: 4def7fb3ab14724bb9cdd921c2c981f31ec8ae19
Branch: microcanonical-refactor

Legacy backends policy: exact fixed-(E,T) rejection/DP code lives in
menobis-test-oracles/src/legacy_fixed_et/ (reference oracle, compiled
and tested on every CI run).  No archive branch.

Current status (after unified router + perf work):

Tests:
  cargo test -p menobis-core         = 286 passed (fast unit tests)
  cargo test -p menobis-test-oracles = 40 passed (enumeration + legacy comparison)
  cargo clippy --workspace -D warnings = clean
  cargo fmt --all -- --check = clean
  Python: targeted routing suites pass (67+ tests); full suite ~385 tests
  (run before any merge to master)

Microcanonical modules:

  fixed_et/         — fixed-(E,T) thin orchestrator calling Gibbs chain
  fixed_kt/         — fixed-degree support MCMC + Gibbs occupation
  fixed_total/      — shared pair-Gibbs fixed-total chain (O(E) memory)
  fixed_strength/   — fixed-strength MCMC (allocation-free cycle4 hot path;
                      dead apply_deltas removed)
  mcmc/             — shared config/counters/outcome types
  support/          — shared uniform-edge sampling
  model/            — shared primitives (family, problem, sampling_plan)
  route/            — Rust router: sample_microcanonical (MC)

Grand-canonical:
  grandcanonical/   — GrandCanonicalCase + sample_grandcanonical router
                      (centralizes the 24 per-family functions)

Python:
  routing.py        — route_model/fit_model/sample_model/filter_model;
                      _sample_model routes GC/canonical/MC through the
                      unified Rust pyfunction sample_model(ensemble,
                      family, constraint, ...) with Rust-side
                      constraint-required parameter validation
  generation.py     — _sample_model_router thin wrapper

Performance fixes applied:
  - fixed_kt complement inversion: dropped hidden O(N²) adjacency memory
    (DegreeSupportState::replace_edges; only O(E) edge list remains)
  - fixed_strength cycle4: allocation-free hot path (4-cycle deltas never
    overlap, removed 3 HashMap allocations per proposal)
  - fixed_strength: removed dead apply_deltas (reworked tests to set())

Reference oracles (menobis-test-oracles):
  enumeration.rs        — exact state enumeration + normalization
  legacy_fixed_et/      — legacy rejection/DP backends (scientific reference)
  tests/fixed_total_stationarity.rs       — Gibbs vs enumeration (TV < 0.03)
  tests/fixed_total_legacy_comparison.rs  — Gibbs vs legacy DP (E=10)

Known limitations / next steps:
  - fixed_strength/ still uses old distribution::OccupationFamily; migrate
    to model::family::OccupationFamily (mechanical, ~8 files)
  - SamplingPlan::classify needs an explicit rule for future fixed-(E,s)
  - Fixed (E,s) not implemented (deferred)
  - Python full suite + benchmark run outstanding before merge gates
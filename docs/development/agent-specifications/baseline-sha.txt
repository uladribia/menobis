Baseline SHA: 4def7fb3ab14724bb9cdd921c2c981f31ec8ae19
Branch: microcanonical-refactor
Date: 2025-07-18

Tests:
  cargo test -p menobis-core    = 268 passed (fast unit tests)
  cargo test -p menobis-test-oracles = 15 passed (heavy enumeration)
  cargo clippy --workspace -D warnings = clean
  cargo fmt --all -- --check = clean

Current microcanonical production code:

  fixed_et/         — ME/B/W fixed-(E,T) via rejection + DP fallback
  fixed_kt/         — fixed-degree support MCMC + reuse of fixed_et occupation
  fixed_strength/   — fixed-strength MCMC (ME direct + 4-cycle)
  mcmc/             — shared config/counters/outcome types
  support/          — shared uniform-edge sampling

  model/            — NEW (Phase 1+2): shared primitives
    family.rs, problem.rs, sampling_plan.rs

  crates/menobis-test-oracles/ — NEW (Phase 3): heavy test infrastructure

Known limitations:
  - No shared fixed-total pair-Gibbs chain (still rejection/DP in fixed_et)
  - Complement mode in fixed_kt/sampler.rs uses O(N^2) iteration
  - No SamplingPlan routing wired into generation entry points
  - Fixed (E,s) not implemented
  - Per-constraint mini-framework structure persists